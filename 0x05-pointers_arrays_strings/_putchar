#include <unistd.h>

/**
_putchar - write the character c to stdout
@c: The charactor to print

Return: 0 on seccess
On error, -1 is returned, and error is set appropretely
*/

_putchar(char c)
return (write(1, &c, 1));
